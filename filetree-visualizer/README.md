# filetree-visualizer

Reusable Rust library for filtering and rendering hierarchies produced by `filetree-maker`.

It supports styled ASCII output, plain JSON serialization, and configurable collapse or omission rules without performing filesystem scans itself.
